<!-- HEADER -->
<header class="main-header py-2">
  <div class="">
    <div class="row">
      <div class="col">
        <div class="text-center">
          <h1><%= link_to 'Borderline Basic', home_path %></h1>
          <p>The chronicles of a Borderline Basic B*tch</p>
        </div>
      </div>
    </div>
  </div>
</header>


<% @posts.sort.reverse.each do |post| %>
  <div class="bg-light mb-3 p-2">
    <h3><%= link_to post.title , post_path(post)%></h3>
    <p><%= post.body %></p>
  </div>
<% end %>

