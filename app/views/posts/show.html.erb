<!-- HEADER -->
<header class="main-header py-2">
  <div class="">
    <div class="row">
      <div class="col">
        <div class="text-center">
          <h1><%= link_to 'Borderline Basic', home_path %></h1>
          <p>The chronicles of a Borderline Basic B*tch</p>
        </div>
      </div>
    </div>
  </div>
</header>

<h2><%= @post.title %></h2>
<p><%= @post.body %></p>
<hr>
<%= link_to "Edit", edit_post_path(@post), :class => 'btn btn-outline-primary'%>
<%= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Are you sure you would like to delete this post?"}, :class => 'btn btn-outline-danger'%>

<hr>



<%= render 'comments/form' %>
<%= render 'comments/comments' %>
